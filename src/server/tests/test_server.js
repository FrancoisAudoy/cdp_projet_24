'use strict'

const assert = require('assert');

describe("Server", () => {
    describe("#TODO", () => {
	it("TODO", () => {
	    assert.equal(1,2);
	});
	it('should pass', () =>{
	    assert(true,"Test à réaliser");
	});
    });
});
